<main class="container">
    <h1 class="text-center">Welcome to ICE 10 INFT2202</h1>
    <p class="lead text-center">This is a simple Express app with Handlebars templating.</p>

    <div class="row">
        <div class="col-md-12">
            <h2>Users</h2>
            <div class="row">
                {{#each users}}
                    <div class="col-md-3">
                        <div class="card mb-3">
                            <div class="card-body">
                                <h5 class="card-title"><a href="/{{this._id}}">{{this.first_name}} {{this.last_name}}</a></h5>
                                <p class="card-text">{{this.email}}</p>
                            </div>
                        </div>
                    </div>
                {{/each}}
            </div>
        </div>
    </div>
</main>
